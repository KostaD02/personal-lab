services:
  excalidraw:
    container_name: excalidraw
    image: excalidraw/excalidraw:latest
    restart: unless-stopped
    ports:
      - "3001:80"
    networks:
      - proxy-nw

networks:
  proxy-nw:
    external: true
