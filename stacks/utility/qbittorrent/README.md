# qBittorrent

This settings is for my personal use. You can change it if you want to.

On first login, the username is `admin` and the password is `adminadmin`.

If `adminadmin` isn't working, you can see password in the logs:

```bash
docker logs qbittorrent | grep "password"
```
